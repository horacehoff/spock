fn fib(n) {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fn main() {
    //print(fib(28));

    let x = 1;
    for y in range(0,10) {
        x *= 3;
    }
    print(x);
}